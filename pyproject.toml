[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "high-value-lottery-monitor"
version = "0.1.0"
description = "高額商材の抽選販売を監視するGitHub Actions向けモニター"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "beautifulsoup4>=4.12,<5",
  "google-api-python-client>=2.160,<3",
  "google-auth>=2.38,<3",
  "requests>=2.32,<3",
]

[project.optional-dependencies]
test = [
  "pytest>=8.3,<9",
  "responses>=0.25,<1",
]
pc = [
  "playwright>=1.50,<2",
  # WindowsはIANAタイムゾーン情報を標準搭載しないため、Asia/Tokyo用に同梱する。
  "tzdata>=2025.2",
]

[project.scripts]
lottery-monitor = "high_value_lottery_monitor.cli:main"
ricoh-entry-assistant = "high_value_lottery_monitor.pc_prepare:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-ra"
